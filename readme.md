Run `docker build -t aws-apache-sample .` to create an image
Run `docker run -dp 8999:80 aws-apache-sample` to create a container. This will run the reviously create image in detached mode on port 8999 
